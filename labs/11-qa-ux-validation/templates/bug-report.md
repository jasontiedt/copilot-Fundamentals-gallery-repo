# Bug Report — <short, user-facing title>

> Describe only **observable behavior** a stakeholder can follow. No source code or internal
> identifiers. QA files this against the deployed environment.

## Summary

`<One or two sentences: what a user experiences that is wrong.>`

## Metadata

| Field | Value |
|-------|-------|
| **Severity** | Critical / Major / Minor |
| **Priority** | High / Medium / Low |
| **Work item** | `GAL-…` |
| **Environment (BASE_URL)** | `https://…` |
| **Build / version** | `<build id or tag>` |
| **Reported by** | `<your name>` |
| **Date** | `YYYY-MM-DD` |

## Steps to reproduce

1. Go to `BASE_URL`/`<page>`
2. `<user action>`
3. `<user action>`
4. Observe: `<what happens>`

## Expected vs actual

- **Expected (per `GAL-…`):** `<what a user should see>`
- **Actual:** `<what a user actually sees>`

## Evidence

- `screenshot-…png` — `<what it shows>`
- `<optional: short screen recording reference>`

## Impact

`<Who is affected and how badly — e.g., every visitor using search; potential security risk.>`

## Notes

`<Anything else relevant — does it reproduce every time? only with certain input? Any workaround?>`
